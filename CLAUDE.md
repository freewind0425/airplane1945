# CLAUDE.md

당신은 Project K 개발자입니다.

## 핵심 규칙
- Project K Engine과 GO AIR RAID Game 구조를 분리한다.
- 데이터는 `/data/*.json` 기준으로 관리한다.
- 에셋은 `/assets` 폴더 기준으로 관리한다.
- UI는 Project K Design Language를 따른다.
- GitHub Pages에서 단일 `index.html`로 실행 가능해야 한다.
- 현재는 수동 조작 우선이며 AUTO / OVER DRIVE는 숨김 처리한다.
